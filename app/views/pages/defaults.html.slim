header.page-title
  h1
    ' Defaults
    small Optional
  code @import 'jetpack/defaults';
  p A small number of obvious, ubiquitous selector definitions.

p Jetpack Defaults are a small set of selectors that you might use
  in your app. The defaults file is not automatically included when
  you use <code>@import 'jetpack';</code>, since it adds css definitions
  you might not want. This is purely a convenience file, you could just
  as easily copy and paste the contents of this file into your application
  manifest.

h4 File Contents
pre
  | // Typographic Styles
    @include text;
    @include headings;
    @include lists;
    @include preformatting;

    // UBIQUITOUS DEFAULTS
    a { color: $color1;
      &:visited { color: $color2; }
    }

    // INTERFACE ELEMENTS
    span.label {
      @include label;
      &.success { @include label($success2,$white); }
      &.notice { @include label($notice2,$white); }
      &.error { @include label($error2,$white); }
    }
    a.button, span.button {
      @include button;
      &.primary { @include button($color1,$white); }
      &.danger { @include button($error2,$white); }
    }
    div.flash-message {
      @include message;
      &.success { @include message($success1,darken($success2,20%)); }
      &.notice { @include message($notice1,darken($notice2,25%)); }
      &.error, &.alert { @include message($error1,darken($error2,10%)); }
    }

    // TABLE STYLES
    table.simple { @include table-style-simple; }
    table.shaded { @include table-style-shaded; }
    table.striped { @include table-style-striped; }

    // NAVIGATION
    div.tab-wrapper { @include tab-style-simple; }
    div.pill-wrapper { @include pill-style-simple; }


